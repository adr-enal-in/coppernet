<?xml version="1.0" encoding="UTF-8"?>
<Response>
  <Gather action="/process-private-menu" method="POST" timeout="10" numDigits="1">
    <Say voice="<%= voice %>">Welcome to Coppernet</Say>
    <Say voice="<%= voice %>">For voicemail, press 1</Say>
    <Say voice="<%= voice %>">To dial out, press 2</Say>
  </Gather>
  <Say voice="<%= voice %>">We didn't receive any input. Goodbye!</Say>
</Response>
